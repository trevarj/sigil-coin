(import (sigil test)
        (sigil coin explorer))

(test-group "sigil-coin-explorer scaffold"
  (test "reports the package version"
    (assert-equal "0.1.0" (explorer-package-version))))

(run-tests)
